package com.orion.finance.interest;

// @RunWith(PowerMockRunner.class)
// @RunWith(MockitoJUnitRunner.class)
// @PrepareForTest({CalendarService.class})
public class InterestServiceTest
{
    /*
     * @Test public void test_getInterest() { Interest result =
     * InterestService.getInterest(InterestRate.of(ANumber.of(0.06)),
     * Capital.of(ANumber.of(100))); Interest expected = Interest.of(ANumber.of(6));
     * assertThat(result).isEqualTo(expected); }
     */
}