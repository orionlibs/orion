package com.orion.finance.interest.simple;

// @RunWith(PowerMockRunner.class)
// @RunWith(MockitoJUnitRunner.class)
// @PrepareForTest({CalendarService.class})
public class SimpleInterestTest
{
    /*
     * @Test public void test_SimpleInterest_getInitialAmount() { SimpleInterest
     * temp = SimpleInterest.of(); temp.setDurationOfInvestment(ANumber.of(10));
     * temp.setFutureValue(Capital.of(ANumber.of(2_000)));
     * temp.setInterestRate(InterestRate.of(ANumber.of(0.1))); Capital result =
     * temp.getInitialAmount(); ANumber expected = ANumber.of(1_000);
     * assertThat(result.getValue()).isEqualTo(expected); }
     * @Test public void test_SimpleInterest_getInterestRate() { SimpleInterest temp
     * = SimpleInterest.of(); temp.setDurationOfInvestment(ANumber.of(10));
     * temp.setFutureValue(Capital.of(ANumber.of(2_000)));
     * temp.setInitialAmount(Capital.of(ANumber.of(1_000))); InterestRate result =
     * temp.getInterestRate(); ANumber expected = ANumber.of(0.1);
     * assertThat(result.getValue()).isEqualTo(expected); }
     * @Test public void test_SimpleInterest_getDurationOfInvestment() {
     * SimpleInterest temp = SimpleInterest.of();
     * temp.setFutureValue(Capital.of(ANumber.of(2_000)));
     * temp.setInitialAmount(Capital.of(ANumber.of(1_000)));
     * temp.setInterestRate(InterestRate.of(ANumber.of(0.1))); ANumber result =
     * temp.getDurationOfInvestment(); ANumber expected = ANumber.of(10);
     * assertThat(result).isEqualTo(expected); }
     * @Test public void test_SimpleInterest_getFutureValue() { SimpleInterest temp
     * = SimpleInterest.of(); temp.setDurationOfInvestment(ANumber.of(10));
     * temp.setInitialAmount(Capital.of(ANumber.of(1_000)));
     * temp.setInterestRate(InterestRate.of(ANumber.of(0.1))); Capital result =
     * temp.getFutureValue(); Capital expected = Capital.of(ANumber.of(2_000));
     * assertThat(result.getValue()).isEqualTo(expected.getValue()); }
     */
}